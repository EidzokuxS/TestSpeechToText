# Speech-to-Text Prototype

Одностраничный прототип с Web Speech API, многоязычным UI и простым чатом.

## Структура

- `index.html` — вся разметка, стили и JS-логика (класс `VoiceInputRecorder`, переключатель языков, чат и т.д.).

## Запуск локально

Браузеры блокируют доступ к микрофону для страниц, открытых по `file://`, поэтому корректно тестировать следует через любой веб-сервер (например, `npx serve`, `python -m http.server`, Live Server в VS Code и т.д.).

## Публикация на GitHub Pages

1. Создайте новый публичный репозиторий, например `speech-to-text-demo`.
2. Скопируйте файлы проекта и запушьте:
   ```bash
   git init
   git add index.html README.md
   git commit -m "Add speech-to-text prototype"
   git remote add origin git@github.com:<username>/speech-to-text-demo.git
   git push -u origin main
   ```
3. В настройках репозитория перейдите в **Settings → Pages**.
4. В блоке **Build and deployment** выберите:
   - **Source**: `Deploy from a branch`
   - **Branch**: `main` / `/ (root)`
   - Сохраните настройки.
5. Через ~1 минуту GitHub создаст страницу по адресу `https://<username>.github.io/speech-to-text-demo/`.
6. Откройте страницу в поддерживаемом браузере (Chrome / Edge / Samsung Internet, частично Safari 14+) — здесь браузер увидит HTTPS и будет кэшировать разрешение микрофона.

## Проверка разрешения микрофона

- После первого нажатия «🎤 Записать» браузер спросит доступ. Выберите «Allow while visiting the site». На HTTPS-домене доступ сохраняется и повторных запросов не должно быть.
- Если доступ отклонён, нужно очистить блокировку в настройках сайта и повторить попытку.

## Поддерживаемые браузеры и ограничения

- Web Speech API официально работает в Chromium-браузерах (Chrome, Edge, Samsung Internet). Safari поддерживает API частично (iOS/macOS 14+). Firefox пока не поддерживает.
- В браузерах без Web Speech API приложение покажет соответствующее сообщение и отключит кнопки.

## TODO / идеи

- Добавить отправку распознанного текста на бэкенд.
- Поддержка принудительного переключения тёмной темы.
- История языковых настроек и горячие клавиши для управления записью.
